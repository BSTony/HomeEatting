services:
  - type: web
    name: feast-sync-display
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: MASTER_PIN
        value: "8888"
